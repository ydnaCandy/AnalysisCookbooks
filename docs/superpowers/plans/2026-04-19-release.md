# リリース構成 実装プラン

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ubuntu上でDocker Composeを使用してアプリを起動できるリリース構成一式を`release/`フォルダに作成する

**Architecture:** nginx（静的配信+APIプロキシ） + FastAPI backend（ホストマウント） + PostgreSQL 16の3サービス構成。ホスト28000番ポートで公開。backendはentrypoint.shでuv sync → alembic migration → uvicorn起動を自動実行。

**Tech Stack:** Docker Compose v2, nginx:alpine, python:3.12-slim, uv, PostgreSQL 16

---

## ファイル一覧

| パス | 種別 | 役割 |
|------|------|------|
| `release/apps/frontend/.gitkeep` | 新規作成 | フロントビルド出力置き場（gitで追跡） |
| `release/apps/backend/.gitkeep` | 新規作成 | バックエンドコード置き場（gitで追跡） |
| `release/backup/.gitkeep` | 新規作成 | pg_dumpバックアップ置き場（gitで追跡） |
| `release/Dockerfile.backend` | 新規作成 | backendイメージ定義（python + uv） |
| `release/entrypoint.sh` | 新規作成 | uv sync → migration → uvicorn起動スクリプト |
| `release/nginx.conf` | 新規作成 | 静的配信 + /api/ プロキシ設定 |
| `release/docker-compose.yml` | 新規作成 | 3サービス定義 |
| `release/.env.example` | 新規作成 | 環境変数テンプレート |
| `release/RELEASE.md` | 新規作成 | リリース・更新手順書 |
| `release/BACKUP.md` | 新規作成 | バックアップ・リストア手順書 |

---

### Task 1: ブランチ作成とフォルダ構造の初期化

**Files:**
- 作成: `release/apps/frontend/.gitkeep`
- 作成: `release/apps/backend/.gitkeep`
- 作成: `release/backup/.gitkeep`

- [ ] **Step 1: 開発ブランチを作成**

```bash
git checkout -b feat/release-config
```

- [ ] **Step 2: フォルダ構造と.gitkeepファイルを作成**

```bash
mkdir -p release/apps/frontend release/apps/backend release/backup
touch release/apps/frontend/.gitkeep
touch release/apps/backend/.gitkeep
touch release/backup/.gitkeep
```

- [ ] **Step 3: 構造確認**

```bash
find release/ -type f
```

期待出力:
```
release/apps/frontend/.gitkeep
release/apps/backend/.gitkeep
release/backup/.gitkeep
```

---

### Task 2: Dockerfile.backend と entrypoint.sh の作成

**Files:**
- 作成: `release/Dockerfile.backend`
- 作成: `release/entrypoint.sh`

- [ ] **Step 1: Dockerfile.backendを作成**

`release/Dockerfile.backend`:
```dockerfile
FROM python:3.12-slim

# uvのインストール
COPY --from=ghcr.io/astral-sh/uv:latest /uv /usr/local/bin/uv

# entrypointをコピー
COPY entrypoint.sh /entrypoint.sh
RUN chmod +x /entrypoint.sh

WORKDIR /app

ENTRYPOINT ["/entrypoint.sh"]
```

- [ ] **Step 2: entrypoint.shを作成**

`release/entrypoint.sh`:
```bash
#!/bin/bash
set -e

cd /app

# 依存関係のインストール
uv sync --no-dev

# DBマイグレーション
alembic upgrade head

# アプリ起動
exec uv run uvicorn app.main:app --host 0.0.0.0 --port 8000
```

- [ ] **Step 3: コミット**

```bash
git add release/
git commit -m "feat: release フォルダ構造・Dockerfile・entrypointを追加"
```

---

### Task 3: nginx.conf の作成

**Files:**
- 作成: `release/nginx.conf`

- [ ] **Step 1: nginx.confを作成**

`release/nginx.conf`:
```nginx
server {
    listen 80;

    # フロントエンド静的ファイル配信
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # バックエンドAPIプロキシ
    location /api/ {
        proxy_pass http://backend:8000/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

- [ ] **Step 2: コミット**

```bash
git add release/nginx.conf
git commit -m "feat: nginx.conf を追加"
```

---

### Task 4: docker-compose.yml の作成

**Files:**
- 作成: `release/docker-compose.yml`

- [ ] **Step 1: docker-compose.ymlを作成**

`release/docker-compose.yml`:
```yaml
services:
  nginx:
    image: nginx:alpine
    ports:
      - "28000:80"
    volumes:
      - ./apps/frontend:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - backend
    restart: unless-stopped

  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    volumes:
      - ./apps/backend:/app
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - SECRET_KEY=${SECRET_KEY}
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  db:
    image: postgres:16
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./backup:/backup
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres-data:
```

- [ ] **Step 2: compose設定の構文チェック**

```bash
cd release/
docker compose config
```

期待: エラーなしでyamlが出力される

- [ ] **Step 3: コミット**

```bash
git add release/docker-compose.yml
git commit -m "feat: docker-compose.yml を追加"
```

---

### Task 5: .env.example の作成

**Files:**
- 作成: `release/.env.example`

- [ ] **Step 1: .env.exampleを作成**

`release/.env.example`:
```
DATABASE_URL=postgresql://postgres:your_password@db:5432/analysis_cookbooks
SECRET_KEY=your-secret-key-here-change-this-in-production
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password
POSTGRES_DB=analysis_cookbooks
```

- [ ] **Step 2: コミット**

```bash
git add release/.env.example
git commit -m "feat: .env.example を追加"
```

---

### Task 6: RELEASE.md（リリース手順書）の作成

**Files:**
- 作成: `release/RELEASE.md`

- [ ] **Step 1: RELEASE.mdを作成**

`release/RELEASE.md`:
```markdown
# リリース手順書

## 前提条件

- Docker Engine がインストール済みであること
- Docker Compose プラグイン（v2）がインストール済みであること

Ubuntu へのインストール:
\```bash
sudo apt-get update
sudo apt-get install -y docker.io docker-compose-v2
sudo usermod -aG docker $USER
\```

## フォルダ構成

\```
release/
  apps/
    frontend/    ← フロントエンドのビルド出力（frontend/dist/ の中身）
    backend/     ← バックエンドのコード（backend/ の中身）
  backup/        ← pg_dump出力先
  docker-compose.yml
  Dockerfile.backend
  nginx.conf
  .env           ← .env.exampleをコピーして設定（要作成）
  RELEASE.md     ← この手順書
  BACKUP.md      ← バックアップ手順書
\```

## 初回セットアップ

### 1. フロントエンドをビルドしてapps/frontendに配置

ローカル開発環境で実行:

\```bash
cd frontend
npm run build
rm -rf ../release/apps/frontend/*
cp -r dist/* ../release/apps/frontend/
\```

### 2. バックエンドをapps/backendに配置

\```bash
rm -rf release/apps/backend/*
cp -r backend/* release/apps/backend/
\```

### 3. releaseフォルダをサーバーに転送

\```bash
scp -r release/ user@server-ip:/opt/analysis-cookbooks/
\```

### 4. サーバーで環境変数を設定

\```bash
cd /opt/analysis-cookbooks
cp .env.example .env
nano .env
\```

以下を必ず変更:
- `SECRET_KEY`: 推測困難なランダム文字列（例: `openssl rand -hex 32` で生成）
- `POSTGRES_PASSWORD`: 任意のパスワード
- `DATABASE_URL`: `POSTGRES_PASSWORD` と合わせること

### 5. 起動

\```bash
docker compose up -d
\```

初回起動時にAlembicマイグレーションが自動実行される。

### 6. 動作確認

\```bash
# 全サービスの状態確認
docker compose ps

# ログ確認（エラーがないか）
docker compose logs --tail=50

# backendのログのみ確認
docker compose logs -f backend
\```

ブラウザで `http://サーバーIP:28000` にアクセスして確認。

---

## アプリの更新

### フロントエンドのみ更新

ローカルで実行:

\```bash
cd frontend
npm run build
rm -rf ../release/apps/frontend/*
cp -r dist/* ../release/apps/frontend/
scp -r release/apps/frontend/* user@server-ip:/opt/analysis-cookbooks/apps/frontend/
\```

nginxコンテナの再起動は不要（マウントファイルが自動反映される）。

### バックエンドのみ更新

ローカルで実行:

\```bash
rm -rf release/apps/backend/*
cp -r backend/* release/apps/backend/
scp -r release/apps/backend/* user@server-ip:/opt/analysis-cookbooks/apps/backend/
\```

サーバーで実行:

\```bash
cd /opt/analysis-cookbooks
docker compose restart backend
\```

コンテナ再起動時にDBマイグレーションが自動実行される。

### 両方を更新

\```bash
# サーバーへ転送後
cd /opt/analysis-cookbooks
docker compose restart backend
\```

---

## 停止・再起動

\```bash
# 全サービス停止（DBデータは保持）
docker compose down

# 全サービス再起動
docker compose restart

# 個別再起動
docker compose restart backend
docker compose restart nginx
\```

## DBデータを含めて完全削除（注意）

\```bash
docker compose down -v
\```

**警告:** `-v` オプションはDBデータが消える。バックアップを取ってから実行すること。
```

- [ ] **Step 2: コミット**

```bash
git add release/RELEASE.md
git commit -m "docs: RELEASE.md（リリース手順書）を追加"
```

---

### Task 7: BACKUP.md（バックアップ・リストア手順書）の作成

**Files:**
- 作成: `release/BACKUP.md`

- [ ] **Step 1: BACKUP.mdを作成**

`release/BACKUP.md`:
```markdown
# バックアップ・リストア手順書

## バックアップ

### 手動バックアップ

\```bash
cd /opt/analysis-cookbooks
docker compose exec db pg_dump -U postgres analysis_cookbooks > backup/backup_$(date +%Y%m%d_%H%M%S).sql
\```

バックアップファイルは `backup/` フォルダに `backup_YYYYMMDD_HHMMSS.sql` 形式で保存される。

### バックアップ一覧の確認

\```bash
ls -lh backup/
\```

### 定期バックアップのcron設定（任意）

\```bash
crontab -e
\```

以下を追加（毎日午前2時に実行）:

\```
0 2 * * * cd /opt/analysis-cookbooks && docker compose exec -T db pg_dump -U postgres analysis_cookbooks > backup/backup_$(date +\%Y\%m\%d).sql
\```

---

## リストア

**注意:** リストアを実行すると既存のデータは上書きされる。必ず作業前にバックアップを取ること。

### 1. リストア前にバックアップを取る

\```bash
cd /opt/analysis-cookbooks
docker compose exec db pg_dump -U postgres analysis_cookbooks > backup/backup_before_restore_$(date +%Y%m%d_%H%M%S).sql
\```

### 2. アプリを停止

\```bash
docker compose stop backend nginx
\```

### 3. DBを再作成

\```bash
docker compose exec db psql -U postgres -c "DROP DATABASE IF EXISTS analysis_cookbooks;"
docker compose exec db psql -U postgres -c "CREATE DATABASE analysis_cookbooks;"
\```

### 4. バックアップからリストア

`backup_YYYYMMDD_HHMMSS.sql` を実際のファイル名に変更して実行:

\```bash
docker compose exec -T db psql -U postgres analysis_cookbooks < backup/backup_YYYYMMDD_HHMMSS.sql
\```

### 5. テーブル確認

\```bash
docker compose exec db psql -U postgres -d analysis_cookbooks -c "\dt"
\```

テーブル一覧が表示されることを確認。

### 6. アプリを再起動

\```bash
docker compose start backend nginx
\```

ブラウザで `http://サーバーIP:28000` にアクセスして動作確認。
```

- [ ] **Step 2: コミット**

```bash
git add release/BACKUP.md
git commit -m "docs: BACKUP.md（バックアップ・リストア手順書）を追加"
```

---

### Task 8: mainへマージ

- [ ] **Step 1: mainにマージ**

```bash
git checkout main
git merge --no-ff feat/release-config -m "feat: Ubuntu向けDocker Composeリリース構成を追加"
```

- [ ] **Step 2: ブランチ削除**

```bash
git branch -d feat/release-config
```
